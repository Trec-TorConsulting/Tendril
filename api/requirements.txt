# Core
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic[email]>=2.0,<3

# Database
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.30,<1
alembic>=1.14,<2

# Auth
python-jose[cryptography]>=3.3,<4
bcrypt>=4.0,<5
authlib>=1.3,<2

# HTTP client
httpx>=0.27,<1

# Scheduler health server
aiohttp>=3.9,<4

# MQTT
aiomqtt>=2.0,<3

# Notifications
pywebpush>=2.0,<3

# Image processing
Pillow>=12.0,<13

# QR code generation
qrcode>=8.0,<9

# Stripe billing
stripe>=8.0,<10

# Multipart form data (file uploads)
python-multipart>=0.0.9,<1

# S3 / MinIO
boto3>=1.34,<2

# PDF report generation
weasyprint>=62.0,<64

# Redis (optional — middleware state for multi-instance deployments)
redis>=5.0,<6

# Prometheus metrics
prometheus-fastapi-instrumentator>=7.0,<8
prometheus-client>=0.20,<1

apiVersion: v1
kind: Service
metadata:
  name: tendril-api
  namespace: tendril
spec:
  selector:
    app: tendril-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tendril-ingress
  namespace: tendril
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: traefik-redirect-https@kubernetescrd,tendril-security-headers@kubernetescrd
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  rules:
  - host: tendril.maddscientist.com  # Replace with your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tendril-web
            port:
              number: 3000
  - host: api.tendril.maddscientist.com  # Replace with your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tendril-api
            port:
              number: 8000
  tls:
  - hosts:
    - tendril.maddscientist.com
    - api.tendril.maddscientist.com
    secretName: tendril-maddscientist-com-tls
